const Breadcrumb = ({ title }) => {
  return (
    <div className="breadcrumbs">
      <div className="container">
        <h2>{title}</h2>
      </div>
    </div>
  );
};

export default Breadcrumb;
